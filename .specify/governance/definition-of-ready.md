<!-- GENERATED by render_views.py — do not edit. Regenerate with: render_views.py --write -->

# Definition of Ready

*Generated from `select_work.py`. Edit the source, not this file.*

| # | A task is ready when… |
|---|---|
| `DOR-001` | Linked to at least one requirement |
| `DOR-002` | Has a named owner |
| `DOR-003` | Assigned to an iteration |
| `DOR-004` | Executable: an L7 task, or a higher leaf that declares a `terminal_reason` |
| `DOR-005` | Every declared dependency exists and is done |
| `DOR-006` | A `test` task names the acceptance criteria it covers |
| `DOR-007` | A `risk-mitigation` task names the risk it reduces |
| `DOR-008` | Every linked requirement is registered and past `DRAFT` |
| `DOR-009` | Every capability contract it names exists (`skills/<name>/SKILL.md`) |

These are applied, not advised. `select_work.py` refuses to return a task that fails any of them, and each refusal is tagged with the rule id above:

```bash
python3 .specify/extensions/openup/scripts/python/select_work.py --json
```

A task that is not ready is not blocked forever — it is blocked until the missing link exists. The reasons are the work.

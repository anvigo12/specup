# Governance Index — lifecycle

> The context map for this directory. An agent navigates `directory → AGENTS.md → index.md →
> the one artifact it needs`, rather than loading the repository.
>
> **Checked by** — `CTX-001` warns when a governed directory has no `index.md`; `CTX-002`
> **fails** when an id named here does not resolve. Nothing checks what this map leaves out.

## Purpose

The Inception artifacts that establish whether this program should exist: the vision and the
people accountable for it.

## Lifecycle position

- Phase: `INCEPTION` — see `.specify/extensions/openup/openup-config.yml` → `lifecycle.phase`
- Iteration: `ITER-I-01`

## Files here

| File | What it settles | Checked by |
|---|---|---|
| `vision.md` | should we build this | `vision_present` — existence only |
| `stakeholders.md` | who is accountable | `stakeholders_identified` — existence only |

## Key identifiers in this scope

- Business objective: `BUS-OBJ-0001`
- Requirements: `REQ-GREET-0001`, `NON-FR-GREET-0001`
- Open high risk: `RISK-0001`
